{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Battery Acceptance Schema",
  "description": "Response returned by the gateway after accepting a valid battery submission.",
  "type": "object",
  "properties": {
    "state": {
      "type": "string",
      "description": "Current lifecycle state immediately after the submission is accepted.",
      "const": "pending"
    },
    "lifecycle_id": {
      "type": "string",
      "description": "Lifecycle identifier for the battery submission",
      "format": "uuid"
    }
  },
  "required": ["state", "lifecycle_id"],
  "additionalProperties": false
}
